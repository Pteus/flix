<h4>Genres</h4>
<ul>
  <% @genres.each do |genre| %>
    <li>
      <%= link_to genre.name, genre %>
    </li>
  <% end %>
</ul>

<% if current_user&.admin? %>
  <section class="admin">
    <%= link_to "New", new_genre_path, class: "button" %>
  </section>
<% end %>